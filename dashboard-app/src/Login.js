import React from 'react';
import { Link } from 'react-router-dom';

const Login = () => {
    return (
    <div className="upper-margin center bt">
    <Link to="/secured"> Login </Link>
  </div>);
}

export default Login;
